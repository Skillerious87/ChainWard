import { ViewLoading } from "@/components/ui/view-loading";

export default function Loading() {
  return <ViewLoading variant="console" title="Loading reward console" hint="Opening the stored schemes" />;
}

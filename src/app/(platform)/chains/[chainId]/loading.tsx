import { ViewLoading } from "@/components/ui/view-loading";

export default function Loading() {
  return <ViewLoading variant="table" title="Rendering chain report" hint="Validating contributions and reward snapshot" />;
}

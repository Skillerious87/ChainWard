import { ViewLoading } from "@/components/ui/view-loading";

export default function Loading() {
  return <ViewLoading variant="table" title="Loading targets" hint="Refreshing target status from Torn" />;
}

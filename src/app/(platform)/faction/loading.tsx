import { ViewLoading } from "@/components/ui/view-loading";

export default function Loading() {
  return <ViewLoading variant="grid" title="Loading faction" hint="Fetching verified faction data" />;
}

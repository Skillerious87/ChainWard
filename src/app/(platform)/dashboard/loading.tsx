import { ViewLoading } from "@/components/ui/view-loading";

export default function Loading() {
  return <ViewLoading variant="grid" title="Loading dashboard" hint="Assembling faction telemetry" />;
}
